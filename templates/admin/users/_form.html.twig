{{ form_start(userForm)}}
    {{ form_row(userForm.lastname) }}
    {{ form_row(userForm.firstname) }}
    {{ form_row(userForm.address) }}
    {{ form_row(userForm.zipcode) }}
    {{ form_row(userForm.city) }}
    {{ form_row(userForm.roles) }}
    <button type="submit" class="btn btn-warning"> {{ button_label|default('Ajouter') }} </button>
{{ form_end(userForm) }}