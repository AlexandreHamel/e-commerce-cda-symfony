<nav class="navbar navbar-expand-lg bg-body-tertiary rounded-bottom-1">
	<div class="container-fluid">
		<a class="navbar-brand h1" href="{{ path('main') }}">Gaming.shop</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('main') }}">Nos produits</a>
				</li>

				{# {% for category in categories %}
					{% if category.parent == null %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{ category.name }}</a>
						{% else %}
							<ul class="dropdown-menu">
								<li>
									<a class="dropdown-item" href="{{ path('categories_list', {slug: category.slug}) }}">{{ category.name }}</a>
								</li>
							</ul>
						</li>
					{% endif %}
				{% endfor %} #}

				{# <li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Périphériques</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="#">écrans</a>
						</li>
						<li>
							<a class="dropdown-item" href="#">Souris</a>
						</li>
						<li>
							<a class="dropdown-item" href="#">Clavier</a>
						</li>
					</ul>
				</li> #}

			</ul>
		</div>

		<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
			{% if app.user %}
				{% if is_granted('ROLE_PRODUCT_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{ path('admin_index') }}">Administration</a>
					</li>
				{% endif %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						{{app.user.firstname}}
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_profile_index') }}">Mon Compte</a>
						</li>
						<li>
							<a class="dropdown-item" href="#">Mon Panier</a>
						</li>
					</ul>
				</li>
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_logout') }}">Déconnexion</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_register') }}">Inscription</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
				</li>
			{% endif %}
		</ul>
	</div>
</nav>

