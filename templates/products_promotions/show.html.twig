{% extends 'base.html.twig' %}

{% block title %}ProductsPromotions{% endblock %}

{% block body %}
    <h1>ProductsPromotions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ products_promotion.id }}</td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ products_promotion.startDate ? products_promotion.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End_date</th>
                <td>{{ products_promotion.endDate ? products_promotion.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Discount</th>
                <td>{{ products_promotion.discount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_products_promotions_index') }}">back to list</a>

    <a href="{{ path('app_products_promotions_edit', {'id': products_promotion.id}) }}">edit</a>

    {{ include('products_promotions/_delete_form.html.twig') }}
{% endblock %}
